[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --color=yes
markers =
    unit: 标记为单元测试 (不需要外部服务)
    integration: 标记为集成测试 (需要模拟服务)
    slow: 标记为耗时测试
    smb: 标记为需要 SMB/NAS 的测试
